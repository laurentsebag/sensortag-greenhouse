var should = require('should'),
    reading = require('../../app/models/reading');

describe('Unit test', function () {
  describe('Model readings', function () {
    it('should have be tested once using mongoose models');
  });
});
